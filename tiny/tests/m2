program copy:

   { Echo-prints the first ten numbers on the input }

var count,x,y: integer;
     
begin
    count := 0;   # initialization
    repeat
	  output (read);
          count := count + 1;
    (count = 3);
x := 9;
output(x);
end copy.
